name: Causal Engine – No False Positives (Release Gate)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  no-false-positives:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install pandas numpy

    - name: Run no false positives (release gate) test
      run: |
        python IMPLEMENTATION/pcb_one_click/stress_test/stress_test_no_false_positives_release.py


name: Gemini Adapter Demo

on: [push]

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: mkdir -p out
      - run: python integrations/gemini/gemini_demo_agent.py
